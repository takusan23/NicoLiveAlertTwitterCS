<Page
    x:Class="NicoLiveAlertTwitterCS.View.AutoAdmissionList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoLiveAlertTwitterCS.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ListView x:Name="auto_admission_listview">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="10">
                        <TextBlock x:Name="nico_fav_listview_textblock" MaxLines="1" Text="{Binding Name}" FontSize="20"/>
                        <Button Content="予約枠自動入場削除" Tag="{Binding Pos}" Click="Button_Click"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
